# Public Assets

Files in this folder are served as static assets by Vite. Place things like:
- Static images (logos, favicons)
- `robots.txt` / `sitemap.xml`
- Example config JSON you want to fetch at runtime

Sample: `sample-banner.txt` below would be available at `/sample-banner.txt`.
